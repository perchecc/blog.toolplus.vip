# Typora 一键发文

1. 在 Typora 写好 Markdown：首行必须是 `# 标题`，标题后的第一段会成为摘要。
2. 将最终稿另存到 `content-inbox/待发布`，双击 [发布博客.cmd](./发布博客.cmd)。

脚本会自动补齐博客信息、检查构建、提交并推送；成功后草稿移到 `content-inbox/已发布`。失败时草稿会留在待发布目录，改完后再次双击即可。
